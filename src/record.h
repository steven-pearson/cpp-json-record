#include "include/Property.h"
#include <json/json.h>

namespace taskly {
  class Record : public INotifyPropertyChange {
  public:
      Record();
      virtual ~Record();
          
  private:

  };
}
